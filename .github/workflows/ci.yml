name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality (Node 24)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check
        run: npm run check

      - name: Test with coverage
        run: npm run test:coverage

      - name: Build package
        run: npm run build

      - name: Inspect package contents
        run: npm pack --dry-run

      - name: Verify lifecycle operations left the repository clean
        shell: bash
        run: |
          status="$(git status --porcelain=v1 --untracked-files=all)"
          if [[ -n "$status" ]]; then
            echo "Build, test, or package lifecycle operations modified the repository:"
            printf '%s\n' "$status"
            exit 1
          fi

  package-smoke:
    name: Package smoke (${{ matrix.os }}, Node ${{ matrix.node }})
    needs: quality
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node: '26'
            run_tests: true
          - os: macos-latest
            node: '24'
            run_tests: true
          - os: windows-latest
            node: '24'
            run_tests: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Test
        if: matrix.run_tests
        run: npm test

      - name: Build package
        run: npm run build

      - name: Pack and execute installed CLI
        shell: bash
        run: |
          package_dir="$RUNNER_TEMP/package-artifact"
          mkdir -p "$package_dir"
          archive="$(npm pack --pack-destination "$package_dir" --json | node -e 'let value=""; process.stdin.on("data", chunk => value += chunk).on("end", () => process.stdout.write(JSON.parse(value)[0].filename))')"
          npm install --global "$package_dir/$archive"
          docsdown --help
          docsdown --version

      - name: Verify lifecycle operations left the repository clean
        shell: bash
        run: |
          status="$(git status --porcelain=v1 --untracked-files=all)"
          if [[ -n "$status" ]]; then
            echo "Build, test, or package lifecycle operations modified the repository:"
            printf '%s\n' "$status"
            exit 1
          fi
